plugins{
    id 'java'
}

apply from:'../build.dependencies.gradle'

group = gradle.properties.get(project_group)
version = gradle.properties.get(project_version)

repositories {
    mavenCentral()
}

dependencies {

}

test {
    useJUnitPlatform()
}